Prefer Serena tools for codebase search and edits. Do not modify files via shell; use structured editing tools or patches. Renderer code uses TypeScript + Vue + Pinia; keep store state as the single source of truth. Cross-process/public types belong in src/preload/types. In renderer, root containers should carry locator classes. For this repo, after finishing a change, at minimum run scope-matched tests plus `pnpm exec tsc -p tsconfig.json --noEmit`; if OrchestraFlow is involved also run `pnpm lint:orchestraflow`.