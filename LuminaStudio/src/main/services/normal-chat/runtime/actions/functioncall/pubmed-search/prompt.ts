export const pubmedSearchActionPrompt = [
  '只有 query 是必填；top_k、sort、date_from、date_to、api_key_ref_id 都可以省略，程序会自动补默认值。',
  '除非你明确需要不同的返回数量、排序或日期过滤，否则不要机械填写这些默认字段。',
  'query 只能填写真正的 PubMed 检索词或检索表达式，不能塞自然语言计划、解释、任务分解或多段文本。',
  '在调用前先分析用户问题，提炼主题实体、同义词/别名、机制词、疾病或表型词、限定条件，再构造 query。不要直接把用户原话整句照抄进去。',
  '当问题宽泛、存在多个证据方向，或需要同时覆盖不同机制/别名/限定条件时，可以在同一轮拆成 2-4 个并行 pubmed_search。',
  '并行查询时，每个 query 必须代表不同检索方向，避免只换词序的重复查询。优先考虑：核心主题词、同义词/别名、机制或 pathway、限定条件。',
  '第一轮并行检索优先覆盖面，不要一开始给每个 query 都加很多过滤器；拿到结果后再决定是否发起第二批补洞查询。',
  '如果问题不是文献检索，就不要硬调用 pubmed-search。'
].join('\n')
